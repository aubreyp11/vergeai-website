@AGENTS.md


## Verge AI Brain (cross-project knowledge base)

Path: `~/Projects/verge-ai/brain/`

When you need context not already in this project:
1. Read `brain/wiki/hot.md` first (recent thinking, fast — ~500 words)
2. Then `brain/wiki/index.md` (master catalog)
3. Drill into `concepts/` or `entities/` subfolders only if needed
4. DO NOT read `brain/` for generic coding tasks or debugging
5. NEVER read `brain/.private/` — isolated content
6. Read-only from this project. Never write to `brain/`.
